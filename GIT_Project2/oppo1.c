#include <stdio.h>

int main()
{
    int z;
    printf("Input the value of z ");
    scanf(" i", &z);
    printf(" 1 * %i = %8i\n", z);
    printf(" 1 * %i = %8i\n", z);
    printf(" 1 * %i = %8i\n", z);
    printf(" 1 * %i = %8i\n", z);
    return 0;
}
